{
  "name": "cadre-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Vercel serverless backend scaffold for Cadre. Providers (Supabase, Cloudinary, Cashfree) are configured but not yet implemented.",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {},
  "devDependencies": {},
  "notes": [
    "Dependencies are intentionally empty in this phase.",
    "When each provider is implemented, add e.g.:",
    "  @supabase/supabase-js  -> services/supabaseService.js",
    "  cloudinary             -> services/cloudinaryService.js",
    "  (Cashfree has no official Node SDK requirement; plain fetch works) -> services/cashfreeService.js"
  ]
}
